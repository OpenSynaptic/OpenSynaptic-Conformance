# Exhaustive Datasets

This directory is for large or aggregate manifests that describe exhaustive certification surfaces.

Typical uses include:

- ecosystem-wide suite manifests
- high-cardinality generated test corpora
- release-level aggregate baselines for L5 certification

The manifest should remain small and versioned even when the underlying generated corpus is too large to commit directly.
