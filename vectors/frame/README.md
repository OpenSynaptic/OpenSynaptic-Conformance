# Frame Vectors

This directory contains canonical byte-level packet vectors.

Expected contents include:

- standard FULL frame layout vectors
- minimum-length frame vectors
- malformed and guard-condition vectors
- future DIFF and HEART frame samples
